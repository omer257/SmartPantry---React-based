{
  "name": "mega",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "express": "^4.15.3",
    "password-generator": "^2.1.0"
  },
  "scripts": {
    "start": "node server.js",
    "test": "cd client && npm install --only=dev && npm install && npm run build"
  }
}